services:
  - type: web
    name: stock-ai-api
    env: python
    plan: free
    buildCommand: bash build.sh
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
